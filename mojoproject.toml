[package]
name = "websocket"
version = "1.1.1"
description = "Pure-Mojo WebSocket client (RFC 6455) with TLS support"
license = "MIT"
platforms = ["linux-64", "osx-arm64"]

[mojo]
requires = ">=1.0.0"

[dependencies]
tls = { git = "Mosaad-M/tls", version = ">=1.4.0" }
tcp = { git = "Mosaad-M/tcp", version = ">=1.1.0" }
url = { git = "Mosaad-M/url", version = ">=1.1.0" }
